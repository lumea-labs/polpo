# ── @polpo-ai/dashboard — self-host config ──────────────────────────────────

# Base URL of your OSS Polpo server (the data plane).
NEXT_PUBLIC_API_URL=http://localhost:4000

# Path prefix the server mounts its API under. The standalone OSS server serves
# at /api/v1; the dashboard views use /v1/... paths, so this prefix is prepended
# (→ /api/v1/...). Leave as-is unless you mounted the server somewhere else.
NEXT_PUBLIC_POLPO_API_PREFIX=/api

# API key for the local server (sk_...). Generate one on your server. Optional
# if the server runs without auth (local dev).
NEXT_PUBLIC_POLPO_API_KEY=
