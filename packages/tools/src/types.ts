/**
 * Types used by tools.
 */

/**
 * Resolved vault credentials for an agent — canonical definition lives in
 * @polpo-ai/core (vault-resolver). Re-exported here for compatibility.
 */
export type { ResolvedVault } from "@polpo-ai/core";
