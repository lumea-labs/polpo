name: Release

on:
  workflow_dispatch:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  id-token: write
  packages: write

jobs:
  test:
    name: Test
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: polpo_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v6
      # Corepack ships with Node 22 and respects `packageManager` in
      # package.json — avoids the pnpm/action-setup action which
      # intermittently 404s on codeload.github.com (2026-05-26).
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - run: corepack enable
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - run: pnpm run build
      - name: Run tests
        run: pnpm run test run
        env:
          TEST_DATABASE_URL: postgresql://postgres:postgres@localhost:5432/polpo_test

  publish:
    name: Publish to npm
    runs-on: ubuntu-latest
    needs: [test]
    steps:
      - uses: actions/checkout@v6
      # Corepack ships with Node 22 and respects `packageManager` in
      # package.json — avoids the pnpm/action-setup action which
      # intermittently 404s on codeload.github.com (2026-05-26).
      - uses: actions/setup-node@v6
        with:
          node-version: 22
      - run: corepack enable
      - uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
          registry-url: https://registry.npmjs.org

      - run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm run build

      - name: Publish @polpo-ai/core
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/core
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/file-stores
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/file-stores
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/vault-crypto
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/vault-crypto
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/drizzle
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/drizzle
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/llm
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/llm
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/connect
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/connect
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/connect-server
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/connect-server
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/connectors
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/connectors
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/channels
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/channels
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/tools
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/tools
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/server
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/server
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/sdk
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/sdk
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/react
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/react
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/dashboard
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/dashboard
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/cli
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/cli
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish @polpo-ai/node
        run: node scripts/publish-package-if-missing.mjs @polpo-ai/node
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Publish polpo-ai
        run: node scripts/publish-package-if-missing.mjs polpo-ai
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

  docker:
    name: Docker Image
    runs-on: ubuntu-latest
    # Wait for npm publish so the Dockerfile's `npm install polpo-ai@${VERSION}`
    # actually finds the packages. Until this dependency was added, docker
    # raced npm publish and intermittently failed with ETARGET.
    needs: [publish]
    steps:
      - uses: actions/checkout@v6
      - uses: docker/setup-buildx-action@v3

      - name: Extract version
        id: version
        run: echo "VERSION=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/runner/Dockerfile
          push: true
          # Daytona sandboxes are x86_64-only — building arm64 burns ~10
          # minutes and then fails on `mount-s3.deb` (hardcoded to
          # /x86_64/ path inside the Dockerfile). Until we switch to a
          # TARGETARCH-aware install or add aarch64 binary URLs, build
          # amd64 only.
          platforms: linux/amd64
          # POLPO_VERSION drives `npm install polpo-ai@${POLPO_VERSION}` inside
          # the Dockerfile. Without this build-arg the Dockerfile fell back to
          # its hardcoded `ARG POLPO_VERSION=0.7.0` and every image we ever
          # pushed (regardless of tag) contained polpo-ai@0.7.0. That's why the
          # Daytona sandbox runtime stayed pinned to 0.7.0 even after months of
          # npm releases.
          build-args: |
            POLPO_VERSION=${{ steps.version.outputs.VERSION }}
          # Image name aligned with what the cloud snapshot script pulls
          # (`ghcr.io/lumea-labs/polpo-runner:<v>`). Before this change the
          # workflow published as `ghcr.io/lumea-labs/polpo:<v>` — different
          # package — so the cloud either 404'd or pulled some manually-
          # published copy that never got refreshed.
          tags: |
            ghcr.io/${{ github.repository_owner }}/polpo-runner:latest
            ghcr.io/${{ github.repository_owner }}/polpo-runner:${{ steps.version.outputs.VERSION }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  release-notes:
    name: Release Notes
    runs-on: ubuntu-latest
    needs: [publish]
    if: always() && needs.publish.result == 'success'
    steps:
      - uses: actions/checkout@v6
      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
