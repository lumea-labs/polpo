/**
 * polpo-ai — public facade.
 * The Node.js runtime lives in @polpo-ai/node (the composition root);
 * this package re-exports it and preserves the historical entry paths.
 */
export * from "@polpo-ai/node";
